<!-- 500 Error Page -->
<section class="error-page" style="min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); margin-top: 76px;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <div class="error-content text-white" data-aos="fade-up">
                    <!-- Error Icon -->
                    <div class="error-icon mb-4">
                        <i class="fas fa-tools" style="font-size: 8rem; opacity: 0.9;"></i>
                    </div>
                    
                    <!-- Error Code -->
                    <h1 class="display-1 fw-bold mb-3" style="font-size: 8rem;">500</h1>
                    
                    <!-- Error Message -->
                    <h2 class="h3 mb-4">Error del Servidor</h2>
                    <p class="lead mb-5">
                        Lo sentimos, algo salió mal en nuestro servidor. Estamos trabajando para solucionarlo.
                    </p>
                    
                    <!-- Action Buttons -->
                    <div class="d-flex gap-3 justify-content-center flex-wrap">
                        <a href="javascript:history.back()" class="btn btn-light btn-lg px-5">
                            <i class="fas fa-arrow-left me-2"></i> Volver
                        </a>
                        <a href="/" class="btn btn-outline-light btn-lg px-5">
                            <i class="fas fa-home me-2"></i> Ir al Inicio
                        </a>
                    </div>
                    
                    <!-- Contact Support -->
                    <div class="mt-5 pt-4" data-aos="fade-up" data-aos-delay="200">
                        <p class="mb-3 opacity-75">Si el problema persiste, contáctanos:</p>
                        <a href="https://wa.me/593993161517?text=Hola,%20estoy%20teniendo%20problemas%20con%20la%20página" 
                           class="btn btn-success btn-lg"
                           target="_blank">
                            <i class="fab fa-whatsapp me-2"></i> Reportar Problema
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
.error-page {
    position: relative;
    overflow: hidden;
}

.error-page::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
        radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
    pointer-events: none;
}

.error-icon {
    animation: shake 3s ease-in-out infinite;
}

@keyframes shake {
    0%, 100% {
        transform: rotate(0deg);
    }
    10%, 30%, 50%, 70%, 90% {
        transform: rotate(-5deg);
    }
    20%, 40%, 60%, 80% {
        transform: rotate(5deg);
    }
}

.error-content {
    position: relative;
    z-index: 1;
}
</style>
